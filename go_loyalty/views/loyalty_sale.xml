<?xml version="1.0" encoding="utf-8"?>

<odoo>

    <record id="view_loyalty_sale_form" model="ir.ui.view">
        <field name="name">loyalty.sale.form</field>
        <field name="model">loyalty.sale</field>
        <field name="arch" type="xml">
            <form string="Loyalty Sale">
                <header>
                    <button name="action_confirm" type="object" string="Confirm" class="oe_highlight"
                            invisible="state != 'draft'"/>

                    <button name="action_cancel" type="object" string="Cancel" class="oe_highlight"
                            invisible="state != 'draft'"/>

                    <field name="state" widget="statusbar" statusbar_visible="draft,confirm,cancel"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" readonly="1"/>
                        </h1>
                    </div>

                    <group>
                        <group string="Customer &amp; Merchant">
                            <field name="partner_id"/>
                            <field name="phone" readonly="1"/>
                            <field name="current_points" readonly="1"/>
                            <field name="merchant_id"/>
                            <field name="merchant_points" readonly="1"/>
                        </group>

                        <group string="Transaction Details">
                            <field name="date"/>
                            <field name="transfer_point_type"/>
                            <field name="points"/>
                            <field name="user_id"/>
                            <field name="rule_id"/>
                        </group>
                    </group>


                    <notebook>
                        <page string="Order Lines">
                            <field name="sale_line_ids">
                                <list string="Products" editable="bottom">
                                    <field name="product_id"/>
                                    <field name="name"/>
                                    <field name="quantity"/>
                                    <field name="price"/>
                                    <field name="discount"/>
                                    <field name="subtotal" readonly="1"/>
                                </list>
                                <form string="Sale Line">
                                    <group>
                                        <field name="product_id"/>
                                        <field name="name"/>
                                        <field name="quantity"/>
                                        <field name="price"/>
                                        <field name="discount"/>
                                        <field name="subtotal" readonly="1"/>
                                    </group>
                                </form>
                            </field>

                            <group name="note_group" col="6" class="mt-2 mt-md-0">
                                <group colspan="4">
                                    <field name="note" nolabel="1" placeholder="Terms and conditions..."/>
                                </group>
                                <group class="oe_subtotal_footer oe_right" colspan="2" name="sale_total">
                                    <field name="amount" widget='monetary'
                                           options="{'currency_field': 'currency_id'}"/>
                                    <field name="discount" widget='monetary'
                                           options="{'currency_field': 'currency_id'}"/>
                                    <field name="extra_merchant_discount" widget='monetary'
                                           options="{'currency_field': 'currency_id'}"/>
                                    <div class="oe_subtotal_footer_separator oe_inline o_td_label">
                                        <label for="final_amount"/>
                                    </div>
                                    <field name="final_amount" nolabel="1" class="oe_subtotal_footer_separator"
                                           widget='monetary' options="{'currency_field': 'currency_id'}"/>
                                </group>
                                <div class="oe_clear"/>
                            </group>

                        </page>
                        <page string="General Information">
                            <group>
                                <group>
                                    <field name="organisation_id" readonly="1"/>
                                    <field name="employee_type" readonly="1"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_loyalty_sale_tree" model="ir.ui.view">
        <field name="name">loyalty.sale.tree</field>
        <field name="model">loyalty.sale</field>
        <field name="arch" type="xml">
            <list string="Loyalty Sales">
                <field name="name"/>
                <field name="partner_id"/>
                <field name="merchant_id"/>
                <field name="amount"/>
                <field name="final_amount"/>
                <field name="state"/>
                <field name="date"/>
            </list>
        </field>
    </record>

    <record id="view_loyalty_sale_search" model="ir.ui.view">
        <field name="name">loyalty.sale.search</field>
        <field name="model">loyalty.sale</field>
        <field name="arch" type="xml">
            <search string="Search Loyalty Sales">
                <field name="name" string="Reference"/>
                <field name="partner_id" string="Customer"/>
                <field name="merchant_id" string="Merchant"/>
                <field name="state" string="Status"/>
                <filter name="draft" string="Draft" domain="[('state','=','draft')]"/>
                <filter name="confirmed" string="Confirmed" domain="[('state','=','confirm')]"/>
                <filter name="cancelled" string="Cancelled" domain="[('state','=','cancel')]"/>
            </search>
        </field>
    </record>


    <record id="action_loyalty_sale" model="ir.actions.act_window">
        <field name="name">Loyalty Sales</field>
        <field name="res_model">loyalty.sale</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_loyalty_sale_tree"/>
        <field name="search_view_id" ref="view_loyalty_sale_search"/>
    </record>


</odoo>
